/node_modules
/.next
/out
/build
.DS_Store
*.pem
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.env*

# Full-res original gallery photos (optimized copies live in public/gallery)
_source/gallery-originals/

# Archived, unrelated scaffold — keep its source in history but never commit
# its dependencies or build output.
_archive/**/node_modules/
_archive/**/dist/

# Separate standalone project — the white-theme site (its own repo: AC-WEB-WHITE)
/ac white web/
